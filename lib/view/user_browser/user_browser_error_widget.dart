import 'package:flutter/material.dart';

class UserBrowserErrorWidget extends StatelessWidget {

  const UserBrowserErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Something went wrong'),
    );
  }
}
